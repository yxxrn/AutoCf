{
    "temp_mail": {
        "provider": "supabase",
        "api_base": "https://YOUR_PROJECT.supabase.co/functions/v1/temp-mail-api",
        "api_key": "YOUR_TEMP_MAIL_API_KEY",
        "domains": ["example.com"]
    },
    "server": {
        "port": 9877,
        "host": "127.0.0.1"
    },
    "_note": "adapter.py currently reads API_BASE / TMK_KEY from env (or built-in defaults). Prefer: set TMK_KEY and API_BASE env vars; do not commit real keys."
}
